runtime: java
env: standard
instance_class: F2

manual_scaling:
  instances: 1

handlers:
  - url: /.*
    script: this field is required, but ignored

env_variables:
  SPRING_PROFILES_ACTIVE: "production"
